#!/usr/bin/python
import setuptools

setuptools.setup(
    name='syngsea',
    version='0.1',
    url='http://github.com/lucylw/synthetic_gsea',
    packages=setuptools.find_packages(),
    install_requires=[
    ],
    tests_require=[
    ],
    zip_safe=False,
    test_suite='py.test',
    entry_points='',
)